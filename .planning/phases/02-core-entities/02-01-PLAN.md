---
phase: 02-core-entities
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/common/dto/pagination.dto.ts
  - src/specialist/dto/create-specialist.dto.ts
  - src/specialist/dto/update-specialist.dto.ts
  - src/specialist/dto/specialist-response.dto.ts
  - src/specialist/dto/specialist-query.dto.ts
  - src/specialist/repositories/specialist.repository.ts
  - src/specialist/specialist.service.ts
  - src/specialist/specialist.controller.ts
  - src/specialist/specialist.module.ts
  - src/app.module.ts
autonomous: true
requirements:
  - SPEC-01
  - SPEC-02
  - SPEC-03
  - SPEC-04
  - SPEC-05
  - API-01
  - API-04
  - API-05

must_haves:
  truths:
    - "User can create a specialist with profile fields (avatar, firstName, lastName, email, phone, description, isTopMaster) without assigning services or locations"
    - "User can update any specialist profile field independently"
    - "User can delete a specialist and cascade deletes clean up relationships automatically"
    - "User can list all specialists for their organization with pagination (skip/take)"
    - "User can get a single specialist by ID scoped to their organization"
    - "All specialist operations enforce organizationId tenant isolation via AccountId decorator"
    - "All inputs are validated via class-validator decorators and return 400 with field-level errors on invalid input"
  artifacts:
    - path: "src/common/dto/pagination.dto.ts"
      provides: "Reusable PaginationDto with skip/take validation"
      exports: ["PaginationDto"]
    - path: "src/specialist/dto/create-specialist.dto.ts"
      provides: "Create specialist input validation"
      exports: ["CreateSpecialistDto"]
    - path: "src/specialist/dto/update-specialist.dto.ts"
      provides: "Update specialist input validation (PartialType)"
      exports: ["UpdateSpecialistDto"]
    - path: "src/specialist/dto/specialist-response.dto.ts"
      provides: "Typed specialist API responses"
      exports: ["SpecialistResponseDto", "SpecialistListResponseDto"]
    - path: "src/specialist/repositories/specialist.repository.ts"
      provides: "Tenant-scoped Specialist data access"
      exports: ["SpecialistRepository"]
    - path: "src/specialist/specialist.service.ts"
      provides: "Specialist business logic with ownership validation"
      exports: ["SpecialistService"]
    - path: "src/specialist/specialist.controller.ts"
      provides: "REST endpoints for specialist CRUD"
      exports: ["SpecialistController"]
    - path: "src/specialist/specialist.module.ts"
      provides: "NestJS feature module wiring"
      exports: ["SpecialistModule"]
  key_links:
    - from: "src/specialist/specialist.controller.ts"
      to: "src/specialist/specialist.service.ts"
      via: "NestJS DI constructor injection"
      pattern: "constructor.*SpecialistService"
    - from: "src/specialist/specialist.service.ts"
      to: "src/specialist/repositories/specialist.repository.ts"
      via: "NestJS DI constructor injection"
      pattern: "constructor.*SpecialistRepository"
    - from: "src/specialist/repositories/specialist.repository.ts"
      to: "src/common/repositories/base.repository.ts"
      via: "extends BaseRepository"
      pattern: "extends BaseRepository"
    - from: "src/specialist/specialist.controller.ts"
      to: "src/common/decorators/account-id.decorator.ts"
      via: "@AccountId() parameter decorator"
      pattern: "@AccountId\\(\\)"
    - from: "src/app.module.ts"
      to: "src/specialist/specialist.module.ts"
      via: "Module imports array"
      pattern: "SpecialistModule"
---

<objective>
Implement complete Specialist CRUD module with repository, service, controller, and DTOs following established NestJS patterns.

Purpose: Enable users to create, read, update, and delete specialists independently without forced relationships to services or locations. This is the primary new entity in Phase 2.

Output: Fully functional SpecialistModule registered in AppModule with 5 REST endpoints (POST, GET list, GET by ID, PUT, DELETE), all tenant-isolated via organizationId.
</objective>

<execution_context>
@/Users/muhemmedibrahimov/.claude/get-shit-done/workflows/execute-plan.md
@/Users/muhemmedibrahimov/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-entities/02-RESEARCH.md
@prisma/schema.prisma
@src/common/repositories/base.repository.ts
@src/common/decorators/account-id.decorator.ts
@src/common/decorators/permission.decorator.ts
@src/common/helpers/validation.helper.ts
@src/appointment/appointment.module.ts
@src/service-duration/service-duration.controller.ts
@src/service-duration/service-duration.module.ts
@src/app.module.ts

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/common/repositories/base.repository.ts:
```typescript
export abstract class BaseRepository {
  constructor(protected readonly prisma: PrismaService) {}
  protected buildWhereWithTenant<T extends Record<string, any>>(where: T, tenantId: string): T & { organizationId: string };
  protected async validateEntityOwnership(entityId: string, tenantId: string, findFn: (id: string) => Promise<{ organizationId: string } | null>): Promise<void>;
  protected async findManyWithTenant<T>(model: any, tenantId: string, where?: Record<string, any>, pagination?: { skip?: number; take?: number }): Promise<{ data: T[]; total: number }>;
}
```

From src/common/decorators/account-id.decorator.ts:
```typescript
export const AccountId = createParamDecorator(/* extracts x-account-id header */);
```

From src/common/decorators/permission.decorator.ts:
```typescript
export type AccountType = 'ALL' | 'COMPANY' | 'USER';
export const Permissions = (permissions: string[], accountType: AccountType = 'ALL') => { /* ... */ };
```

From prisma/schema.prisma (Specialist model):
```prisma
model Specialist {
  id             String   @id @default(uuid())
  organizationId String
  avatar         String?
  firstName      String
  lastName       String
  email          String
  phone          String?
  description    String?
  isTopMaster    Boolean  @default(false)
  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt
  // Relations: schedules, specialistServices, specialistLocations
  @@index([organizationId])
  @@index([organizationId, email])
  @@index([organizationId, isTopMaster])
  @@map("specialists")
}
```

From src/common/fabrics/factory.dto.ts:
```typescript
export class FactoryDto {
  static create<T, K>(dtoClass: ClassConstructor<T>, source: K, transform?: (data: K) => Partial<T>): T;
  static createMany<T, K>(dtoClass: ClassConstructor<T>, sources: K[], transform?: (data: K) => Partial<T>): T[];
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create PaginationDto, Specialist DTOs, and SpecialistRepository</name>
  <files>
    src/common/dto/pagination.dto.ts,
    src/specialist/dto/create-specialist.dto.ts,
    src/specialist/dto/update-specialist.dto.ts,
    src/specialist/dto/specialist-response.dto.ts,
    src/specialist/dto/specialist-query.dto.ts,
    src/specialist/repositories/specialist.repository.ts
  </files>
  <behavior>
    - PaginationDto: skip defaults to 0, take defaults to 20, take max 100, both optional, @Type(() => Number) for query string coercion
    - CreateSpecialistDto: firstName required (1-100 chars), lastName required (1-100 chars), email required (@IsEmail), avatar optional string, phone optional string, description optional (0-1000 chars), isTopMaster optional boolean default false. NO organizationId field.
    - UpdateSpecialistDto: extends PartialType(CreateSpecialistDto), all fields optional
    - SpecialistResponseDto: all Specialist model fields with @ApiProperty decorators (id, organizationId, avatar, firstName, lastName, email, phone, description, isTopMaster, createdAt, updatedAt)
    - SpecialistListResponseDto: data (SpecialistResponseDto[]), total (number), page (number), pageSize (number)
    - SpecialistQueryDto: extends PaginationDto (inherits skip/take)
    - SpecialistRepository: extends BaseRepository, implements create(data, organizationId), findById(id, organizationId), findMany(organizationId, pagination), update(id, data, organizationId), delete(id, organizationId). Uses buildWhereWithTenant for reads, validateEntityOwnership for mutations.
  </behavior>
  <action>
    **1. Create `src/common/dto/pagination.dto.ts`:**
    Reusable pagination DTO with `skip` (default 0, @Min(0), @IsInt, @IsOptional, @Type(() => Number)) and `take` (default 20, @Min(1), @Max(100), @IsInt, @IsOptional, @Type(() => Number)). Include @ApiPropertyOptional decorators with description, example, min/max, default values.

    **2. Create `src/specialist/dto/create-specialist.dto.ts`:**
    - `avatar?: string` — @IsOptional, @IsString, @ApiPropertyOptional
    - `firstName: string` — @IsNotEmpty, @IsString, @Length(1, 100), @ApiProperty
    - `lastName: string` — @IsNotEmpty, @IsString, @Length(1, 100), @ApiProperty
    - `email: string` — @IsNotEmpty, @IsEmail, @ApiProperty
    - `phone?: string` — @IsOptional, @IsString, @ApiPropertyOptional
    - `description?: string` — @IsOptional, @IsString, @Length(0, 1000), @ApiPropertyOptional
    - `isTopMaster?: boolean` — @IsOptional, @IsBoolean, default false, @ApiPropertyOptional
    - CRITICAL: Do NOT include organizationId — extracted from AccountId decorator in controller

    **3. Create `src/specialist/dto/update-specialist.dto.ts`:**
    Import PartialType from `@nestjs/swagger` (not @nestjs/mapped-types, so Swagger metadata is preserved). `export class UpdateSpecialistDto extends PartialType(CreateSpecialistDto) {}`

    **4. Create `src/specialist/dto/specialist-response.dto.ts`:**
    SpecialistResponseDto with all Specialist model fields and @ApiProperty decorators. SpecialistListResponseDto with `data: SpecialistResponseDto[]`, `total: number`, `page: number`, `pageSize: number`.

    **5. Create `src/specialist/dto/specialist-query.dto.ts`:**
    `export class SpecialistQueryDto extends PaginationDto {}` — inherits skip/take from PaginationDto. Can be extended later with filters (e.g., isTopMaster, search).

    **6. Create `src/specialist/repositories/specialist.repository.ts`:**
    @Injectable() class extending BaseRepository. Constructor takes PrismaService, calls super(prisma).
    Methods:
    - `create(data: { avatar?, firstName, lastName, email, phone?, description?, isTopMaster?, organizationId }, organizationId)`: validates organizationId match, calls `this.prisma.specialist.create({ data })`
    - `findById(id, organizationId)`: uses `this.prisma.specialist.findFirst({ where: this.buildWhereWithTenant({ id }, organizationId) })`
    - `findMany(organizationId, pagination)`: uses `this.findManyWithTenant(this.prisma.specialist, organizationId, {}, pagination)`
    - `update(id, data, organizationId)`: calls validateEntityOwnership first (using findFn that does `this.prisma.specialist.findUnique({ where: { id }, select: { organizationId: true } })`), then `this.prisma.specialist.update({ where: { id }, data })`
    - `delete(id, organizationId)`: calls validateEntityOwnership first, then `this.prisma.specialist.delete({ where: { id } })`. Cascade handles relationships.

    Import Prisma types from `prisma/__generated__` following existing codebase convention (see service-duration.repository.ts import).
  </action>
  <verify>
    <automated>cd /Users/muhemmedibrahimov/work/4f/4FRENDS-BACK/booking-appointment-service && npx tsc --noEmit --pretty 2>&1 | head -50</automated>
  </verify>
  <done>
    - PaginationDto exists at src/common/dto/pagination.dto.ts with skip/take validation and @ApiPropertyOptional
    - All 4 specialist DTOs exist with correct class-validator decorators and @ApiProperty annotations
    - SpecialistRepository extends BaseRepository with all 5 CRUD methods using tenant filtering
    - TypeScript compilation passes with no errors
    - No organizationId field in CreateSpecialistDto or UpdateSpecialistDto
  </done>
</task>

<task type="auto">
  <name>Task 2: Create SpecialistService, SpecialistController, SpecialistModule and register in AppModule</name>
  <files>
    src/specialist/specialist.service.ts,
    src/specialist/specialist.controller.ts,
    src/specialist/specialist.module.ts,
    src/app.module.ts
  </files>
  <action>
    **1. Create `src/specialist/specialist.service.ts`:**
    @Injectable() class with constructor injecting SpecialistRepository.
    Methods:
    - `create(dto: CreateSpecialistDto, organizationId: string): Promise<SpecialistResponseDto>` — spreads dto, adds organizationId, calls repository.create. Returns specialist cast as SpecialistResponseDto.
    - `findAll(organizationId: string, skip?: number, take?: number): Promise<SpecialistListResponseDto>` — calls repository.findMany, computes page as `Math.floor((skip || 0) / (take || 20)) + 1`, returns { data, total, page, pageSize: take || 20 }.
    - `findById(id: string, organizationId: string): Promise<SpecialistResponseDto>` — calls repository.findById, throws NotFoundException if null, returns specialist.
    - `update(id: string, dto: UpdateSpecialistDto, organizationId: string): Promise<SpecialistResponseDto>` — calls repository.update (ownership validated in repo), returns specialist.
    - `delete(id: string, organizationId: string): Promise<void>` — calls repository.delete (ownership validated in repo, cascade handles relationships per SPEC-03).

    **2. Create `src/specialist/specialist.controller.ts`:**
    @ApiTags('Specialists') @Controller('specialists') class with constructor injecting SpecialistService.
    Endpoints (follow company-appointment.controller.ts pattern):
    - `@Post() @Permissions(['specialists.manage'], 'COMPANY')` — create(@Body() dto: CreateSpecialistDto, @AccountId() organizationId: string). @ApiOperation({ summary: 'Create new specialist' }), @ApiResponse({ status: 201, type: SpecialistResponseDto }).
    - `@Get() @Permissions(['specialists.view'], 'COMPANY')` — findAll(@Query() query: SpecialistQueryDto, @AccountId() organizationId: string). @ApiOperation({ summary: 'List specialists with pagination' }), @ApiResponse({ status: 200, type: SpecialistListResponseDto }).
    - `@Get(':id') @Permissions(['specialists.view'], 'COMPANY')` — findById(@Param('id') id: string, @AccountId() organizationId: string). @ApiParam({ name: 'id' }), @ApiResponse({ status: 200, type: SpecialistResponseDto }).
    - `@Put(':id') @Permissions(['specialists.manage'], 'COMPANY')` — update(@Param('id') id: string, @Body() dto: UpdateSpecialistDto, @AccountId() organizationId: string). @ApiResponse({ status: 200, type: SpecialistResponseDto }).
    - `@Delete(':id') @Permissions(['specialists.manage'], 'COMPANY')` — delete(@Param('id') id: string, @AccountId() organizationId: string). @ApiResponse({ status: 200 }). Returns void. Do NOT set @HttpCode(204) — follow existing pattern (service-duration uses default 200 for delete).

    **3. Create `src/specialist/specialist.module.ts`:**
    @Module with imports: [PrismaModule], controllers: [SpecialistController], providers: [SpecialistService, SpecialistRepository], exports: [SpecialistService]. Follow exact pattern from service-duration.module.ts.

    **4. Update `src/app.module.ts`:**
    Add `import { SpecialistModule } from './specialist/specialist.module';` at top.
    Add `SpecialistModule` to imports array after `ServiceDurationModule`.
    Do NOT remove any existing imports.
  </action>
  <verify>
    <automated>cd /Users/muhemmedibrahimov/work/4f/4FRENDS-BACK/booking-appointment-service && npx tsc --noEmit --pretty 2>&1 | head -50</automated>
  </verify>
  <done>
    - SpecialistService exists with 5 CRUD methods (create, findAll, findById, update, delete)
    - SpecialistController exists with 5 REST endpoints matching NestJS patterns (POST, GET, GET/:id, PUT/:id, DELETE/:id)
    - All endpoints use @Permissions decorator with 'COMPANY' account type and @AccountId() for organizationId extraction
    - All endpoints have @ApiOperation and @ApiResponse decorators for Swagger
    - SpecialistModule exists with correct imports/controllers/providers/exports
    - AppModule imports SpecialistModule
    - TypeScript compilation passes with no errors
    - Specialist can be created without assigning services or locations (SPEC-01)
    - Delete operation relies on Prisma cascade for relationship cleanup (SPEC-03)
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — TypeScript compilation passes with zero errors
2. Verify file structure: `ls -la src/specialist/ src/specialist/dto/ src/specialist/repositories/` — all expected files exist
3. Verify PaginationDto: `ls src/common/dto/pagination.dto.ts` — shared DTO exists
4. Verify AppModule registration: `grep 'SpecialistModule' src/app.module.ts` — module is imported
5. Verify no organizationId in DTOs: `grep -r 'organizationId' src/specialist/dto/` — should only appear in response DTO, NOT in create/update DTOs
6. Verify BaseRepository usage: `grep 'extends BaseRepository' src/specialist/repositories/specialist.repository.ts` — repository extends base
7. Verify tenant filtering: `grep 'buildWhereWithTenant\|validateEntityOwnership' src/specialist/repositories/specialist.repository.ts` — both helpers used
</verification>

<success_criteria>
- All 10 files created/modified as listed in files_modified
- TypeScript compilation passes with zero errors
- Specialist CRUD endpoints accessible at /specialists (POST, GET, GET/:id, PUT/:id, DELETE/:id)
- All endpoints enforce organizationId via @AccountId() decorator (no organizationId in request body DTOs)
- Repository uses BaseRepository helpers for tenant isolation
- PaginationDto reusable for other entities (Service, Location)
- Swagger documentation auto-generated from @ApiProperty and @ApiResponse decorators
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-entities/02-01-SUMMARY.md`
</output>
